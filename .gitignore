# 构建产物
classes/
target/
*.class

# 本地编译用的依赖 jar（通过 Maven 或自行下载）
paper-api.jar
adventure-api.jar
adventure-key.jar
examination-api.jar
annotations.jar
bungee-chat.jar
guava.jar

# IDE
.idea/
.vscode/
*.iml
.eclipse/
bin/

# 构建产物 jar 请通过 GitHub Releases 发布
ArchitectBlocks-*.jar

# 临时文件
resp.json
body.json

# 外站专用文档（不入仓库）
README_EXTERNAL.md
